[project]
name = "evals-framework"
version = "0.1.0"
description = "Evaluation and Benchmarking Framework for LLMs, MCP Tools, and Agent Skills"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "rich>=13.7.0",
    "pydantic>=2.0.0",
    "httpx>=0.27.0",
    "mcp>=2.0.0"
]
